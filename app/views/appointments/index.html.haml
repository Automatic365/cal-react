%h1 React Calendar

%h2 Appointments

%h3 Make new appointments

= form_for @appointment, remote: true do |f|
  = f.text_field :title, placeholder: "Appointment Title"
  = f.text_field :appt_time, placeholder: "Appointment Date"
  = f.submit "Make Appointment"

#appointments
= render 'appointments'
